# generic ignores
/.bundle
/log/*.log
/tmp

# Mac ignores
.DS_Store

# don't git track the compile assets
public/assets

# data dumps
/data

# file with all the application secrets
# apart of the Figaro gem
config/application.yml
config/application.staging.yml
config/application.production.yml

# ! means that we want to add this file to git
#
# make sure we provide an example application.yml file
!config/application.example.yml

# copy the sample file instead
config/database.yml
